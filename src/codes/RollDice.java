package codes;

import java.util.LinkedList;
import java.util.List;
import java.util.Scanner;
import java.io.PrintWriter;

public class RollDice {
    public void solve(int testNumber, Scanner in, PrintWriter out) {

    }

//    public int numRollsToTarget(int d, int f, int target) {
//
//    }
//
//    private static void sum_up(List<Integer> numbers, Integer target)
//    {
//        sum_up_recursive(numbers, target, new MyLinkedList.java<>());
//    }

//    private static void sum_up_recursive(List<Integer> numbers, Integer target, List<Integer> partial)
//    {
//        int s = 0;
//        foreach (int x in partial) s += x;
//
//        if (s == target)
//            Console.WriteLine("sum(" + string.Join(",", partial.ToArray()) + ")=" + target);
//
//        if (s >= target)
//            return;
//
//        for (int i = 0; i < numbers.Count; i++)
//        {
//            List<int> remaining = new List<int>();
//            int n = numbers[i];
//            for (int j = i + 1; j < numbers.Count; j++) remaining.Add(numbers[j]);
//
//            List<int> partial_rec = new List<int>(partial);
//            partial_rec.Add(n);
//            sum_up_recursive(remaining, target, partial_rec);
//        }
//    }
}
